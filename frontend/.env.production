# Used by `npm run build` when deploying static assets (e.g. Firebase via scripts/deploy-production.ps1).
# Full backend base URL including /api path.
VITE_API_URL=https://anti-doom-scroll-557363007776.europe-west1.run.app/api
#
# Monolith Docker image: Dockerfile sets VITE_API_URL= at build time so the bundled app uses same-origin /api.
